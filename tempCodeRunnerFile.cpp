 h1.insert(60);
    h1.insert(50);
    h1.insert(40);
    h1.insert(30);
    h1.insert(20);
    h1.insert(55);
    h1.print();
    h1.insert(65);
    h1.print();
    //for insertion TC O(logn) as we're going to top of tree from bottom, i.e.height of tree
    h1.del();
    h1.print();